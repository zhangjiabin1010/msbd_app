import 'package:flutter/material.dart';

class DiscussRecommend extends StatelessWidget {
  const DiscussRecommend({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Column(children: [
      Text("推荐")
    ]));
  }
}


